def deltafunc(s,u,t,v):
    if delta[s][u][t][v] is None:
        # then put this vector into stack
        deltaStack.push([s,u,t,v])
    
    while len(deltaStack) != 0:
        q = deltaStack.peek()
        if setDelta(q) = True:
            deltaStack.pop()


def setDelta(s,u,t,v):
    sb = deltaSbAb()[0]
    ab = deltaSbAb()[1]
    
    if sb and ad have values:
        # set those in matrix
        if sb > ab:
            pass
        else:
            pass

def deltaSbAb():
    dv1 = 
    dv2 = 
    dv3 = 
    dv4 = 

    if dv1 is None:
        # push dv1 into stack
        deltaStack.append(dv1)

    ...
    
    if one of the dvn is None:
        # return None
    
