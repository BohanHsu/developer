## This R script is for algo prob4
## As the problem said:
### Minimize the sum of three numbers x, y and z such that
### 5x + z is at least 15.
### x + 5y is at least 5.
### 4x + y is at most 10.
### y + 3z is at most -20.

# the standard format of this problem is:
# -5 * x +  -1 * y + 0 * z <= -15
# -1 * x + -5 * x + 0 * z <= -5
# 4 * x + y + 0 * z <= 10
# 0 * x + y + 3 * z <= -20

# the object function is:
# minimize:x + y + z

# The R code is:

cvec <- c(1,1,1)
names(cvec) <- c("x","y","z")

bvec <- c(-15,-5,10,-20)

Amat <- rbind(	c(-5,-1,0),
		c(-1,-5,0),
		c(4,1,0),
		c(0,1,3))

res = linprog::solveLP(cvec,bvec,Amat,FALSE)

print(res)