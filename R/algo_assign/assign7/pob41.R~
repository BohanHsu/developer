## This R script is for algo prob4
## As the problem said:
### Minimize the sum of three numbers x, y and z such that
### 5x + z is at least 15.
### x + 5y is at least 5.
### 4x + y is at most 10.
### y + 3z is at most -20.

# because x, y, z maybe negative
# change x to x1 - x2
# change y to y1 - y2
# change z to z1 - z2


# the standard format of this problem is:

# -5 * (x1-x2) + 0 * (y1-y2) + -1 * (z1-z2) <= -15
# -1 * (x1-x2) + -5 * (y1-y2) + 0 * (z1-z2) <= -5
# 4 * (x1-x2) + (y1-y2) + 0 * (z1-z2) <= 10
# 0 * (x1-x2) + (y1-y2) + 3 * (z1-z2) <= -20

# -5 * x1 + 5 * x2 + 0 * y1 + 0 * y2 + -1 * z1 + 1 * z2 <= -15
# -1 * x1 + 1 * x2 + -5 * y1 + 5 * y2 + 0 * z1 + 0 * z2 <= -5
# 4 * x1 + -4 * x2 + 1 * y1 + -1 * y2 + 0 * z1 + 0 * z2 <= 10
# 0 * x1 + 0 * x2 + 1 * y1 + -1 * y2 + 3 * z1 + -3 * z2 <= -20

# the object function is:
# minimize:x + y + z
# minimize: x1 - x2 + y1 - y2 + z1 - z2

# The R code is:

cvec <- c(1,-1,1,-1,1,-1)
names(cvec) <- c("x1","x2","y1","y2","z1","z2")

bvec <- c(-15,-5,10,-20)
names(bvec) <- c("cons1","cons2","cons3","cons4")

Amat <- rbind(	c(-5,5,0,0,-1,1),
		c(-1,1,-5,5,0,0),
		c(4,-4,1,-1,0,0),
		c(0,0,1,-1,3,-3))

res = linprog::solveLP(cvec,bvec,Amat,FALSE)

print(res)